Extract payment receipt data from this image. Return ONLY a JSON object, nothing else.

Fields to extract:
- amount_found: Transfer amount as a number (no currency symbols)
- recipient_account: Recipient/payee account name
- reference_found: The "Recipient Reference" field (typically starts with "BUS", e.g. BUS123456)
- transaction_time: Date and time in format "YYYY-MM-DD HH:MM"

Rules:
- Use null for any field you cannot find
- Do NOT include any text before or after the JSON
- Do NOT use markdown code blocks

Output ONLY this JSON format:
{"amount_found": 45.00, "recipient_account": "COMPANY NAME", "reference_found": "BUS123456", "transaction_time": "2026-03-23 14:30"}

/no_think
